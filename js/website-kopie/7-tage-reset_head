
<style>
/* <![CDATA[ */

/* =========================================================
   1. JIMDO CLEANUP – NUR FÜR DIESE SEITE
========================================================= */


#page-2724813135 .jtpl-navigation__checkbox,
#page-2724813135 .jtpl-mobile-fallback,
#page-2724813135 .jtpl-header,
#page-2724813135 header,
#page-2724813135 .jtpl-navigation,
#page-2724813135 .navigation,
#page-2724813135 .j-nav-variant-nested,
#page-2724813135 .jtpl-navigation__menu-icon,
#page-2724813135 .jtpl-sidebar,
#page-2724813135 footer,
#page-2724813135 .jtpl-footer,
#page-2724813135 .cc-scroll-top {
  display: none !important;
  visibility: hidden !important;
  opacity: 0 !important;
  height: 0 !important;
  min-height: 0 !important;
  max-height: 0 !important;
  margin: 0 !important;
  padding: 0 !important;
  overflow: hidden !important;
  border: 0 !important;
} 

#page-2724813135 .jtpl-section.cc-content-parent > header,
#page-2724813135 .jtpl-section.cc-content-parent,
#page-2724813135 .jtpl-main.cc-content-parent {
  margin-top: 0 !important;
  padding-top: 0 !important;
  top: 0 !important;
}

#page-2724813135 .jtpl-main.cc-content-parent {
  position: relative;
  margin-top: -2px !important;
}

#page-2724813135 .jtpl-main,
#page-2724813135 .jtpl-content,
#page-2724813135 .cc-container,
#page-2724813135 .cc-inner {
  width: 100% !important;
  max-width: 100% !important;
  margin: 0 !important;
  padding: 0 !important;
} 

/* =========================================================
   2. DESIGN SYSTEM
========================================================= */
:root {
  --sage: #8a9e8c;
  --sage-light: #c4d4c6;
  --sage-deep: #4a6b4e;
  --cream: #f7f4ef;
  --warm-white: #fdfcfa;
  --charcoal: #2c2c2c;
  --mid: #5a5a5a;
  --soft: #9a9a9a;
  --accent: #c4956a;
  --accent-light: #e8d5c0;
  --red-soft: #c47a7a;
  --yellow-soft: #c4a84a;
  --green-soft: #6a9e7a;
  --r: 8px;
}

/* =========================================================
   3. RESET & BASE
========================================================= */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html { scroll-behavior: smooth; }

body {
  font-family: 'DM Sans', sans-serif;
  background: var(--warm-white);
  color: var(--charcoal);
  overflow-x: hidden;
}
html, body {
  margin: 0 !important;
  padding: 0 !important;
  background: var(--warm-white) !important;
}

#page-2724813135 {
  margin-top: -4px !important;
}

#page-2724813135 .hero {
  margin-top: -4px !important;
}

html {
  -webkit-text-size-adjust: 100%;
  text-size-adjust: 100%;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* =========================================================
   4. GLOBAL TYPOGRAPHY
========================================================= */
.section-label {
  font-size: 11px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--sage);
  margin-bottom: 16px;
  display: block;
}

.section-h2 {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(32px, 4vw, 52px);
  font-weight: 300;
  line-height: 1.15;
  color: var(--charcoal) !important;
  margin-bottom: 24px;
}

.section-h2 em {
  font-style: italic;
  color: var(--sage-deep);
}

.section-text {
  font-size: 16px;
  line-height: 1.8;
  color: var(--mid);
  font-weight: 300;
}

/* Sonderbereiche mit dunklem Hintergrund */
.score-section .section-h2 {
  color: var(--cream) !important;
}

.score-section .section-h2 em {
  color: var(--accent-light) !important;
}

.result .section-h2 {
  color: var(--cream) !important;
}

.result .section-h2 em {
  color: var(--accent-light) !important;
}

.final-cta .section-h2 {
  color: var(--charcoal) !important;
}

.final-cta .section-h2 em {
  color: var(--sage-deep) !important;
}

.score-section .section-text {
  color: rgba(247,244,239,0.65) !important;
}

.score-section .section-label,
.result .section-label {
  color: var(--sage-light) !important;
}

/* =========================================================
   5. SECTION / CONTAINER SYSTEM
========================================================= */
section:not(.hero) {
  padding: 50px 40px;
}

.hero {
  padding: 0;
  min-height: 100vh;
  display: grid;
  grid-template-columns: 1fr 1fr;
  position: relative;
  overflow: hidden;
  width: 100vw;
  margin-left: calc(50% - 50vw);
}
.why,
.days,
.score-section,
.audience,
.objections,
.offer,
.result,
.final-cta {
  width: 100vw;
  margin-left: calc(50% - 50vw);
}

.container {
  max-width: 1040px;
  margin: 0 auto;
  padding: 0 24px;
}

.container-narrow {
  max-width: 760px;
  margin: 0 auto;
}

/* =========================================================
   6. HERO
========================================================= */
.hero-left {
  background: var(--sage-deep);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 80px 80px;
  position: relative;
  overflow: hidden;
  color: var(--cream);
}

.hero-left::before {
  content: '';
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse at 30% 60%, rgba(196,149,106,0.18) 0%, transparent 60%),
    radial-gradient(ellipse at 80% 20%, rgba(138,158,140,0.25) 0%, transparent 50%);
}

.hero-left::after {
  content: '';
  position: absolute;
  bottom: -60px;
  right: -60px;
  width: 300px;
  height: 300px;
  border-radius: 50%;
  border: 1px solid rgba(196,149,106,0.2);
}

.hero-left-inner {
  position: relative;
  z-index: 2;
  width: 100%;
  max-width: 560px;
  margin-left: auto;
  margin-right: 48px;
}

.hero-right {
  background: var(--cream);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 80px 80px;
  position: relative;
}

.identify-card {
  width: 100%;
  max-width: 500px;
  margin-left: 48px;
  margin-right: auto;
  opacity: 0;
  animation: fadeUp 0.8s 0.5s forwards;
}

/* =========================================================
   7. HERO TYPO & CTA
========================================================= */
.hero-tag {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(196,149,106,0.2);
  border: 1px solid rgba(196,149,106,0.4);
  color: var(--accent-light);
  font-size: 11px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  padding: 6px 14px;
  border-radius: 100px;
  margin-bottom: 36px;
  opacity: 0;
  animation: fadeUp 0.8s 0.2s forwards;
}

.hero-tag::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--accent);
}

.hero-h1 {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(52px, 6.2vw, 92px);
  font-weight: 300;
  line-height: 1.1;
  color: var(--cream) !important;
  margin-bottom: 24px !important;
  opacity: 0;
  animation: fadeUp 0.8s 0.4s forwards;
}

.hero-h1 em {
  font-style: italic;
  color: var(--accent-light);
}

.hero-sub {
  font-size: 17px;
  max-width: 520px;
  line-height: 1.7;
  color: rgba(247,244,239,0.7);
  font-weight: 300;
  margin-bottom: 48px;
  opacity: 0;
  animation: fadeUp 0.8s 0.6s forwards;
}

.hero-cta-group {
  opacity: 0;
  animation: fadeUp 0.8s 0.8s forwards;
}

.btn-primary {
  display: inline-block;
  background: var(--accent);
  color: #fff !important;
  font-family: 'DM Sans', sans-serif;
  font-size: 17px;
  font-weight: 500;
  padding: 18px 40px;
  border-radius: 4px;
  text-decoration: none;
  letter-spacing: 0.02em;
  transition: all 0.25s;
  cursor: pointer;
  border: none;
  position: relative;
  overflow: hidden;
}
#page-2724813135 a.btn-primary,
#page-2724813135 a.btn-primary:link,
#page-2724813135 a.btn-primary:visited,
#page-2724813135 a.btn-primary:hover,
#page-2724813135 a.btn-primary:active,
#page-2724813135 a.btn-primary:focus {
  color: #ffffff !important;
  text-decoration: none !important;
}

.btn-primary::after {
  content: '';
  position: absolute;
  inset: 0;
  background: rgba(255,255,255,0);
  transition: background 0.2s;
}

.btn-primary:hover::after {
  background: rgba(255,255,255,0.1);
}

.btn-primary:hover {
  transform: translateY(-1px);
  box-shadow: 0 8px 24px rgba(196,149,106,0.4);
}

.price-hint {
  display: block;
  margin-top: 12px;
  font-size: 13px;
  color: rgba(247,244,239,0.5);
  letter-spacing: 0.04em;
}

/* =========================================================
   8. IDENTIFY
========================================================= */
.identify-label {
  font-size: 11px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--sage-deep);
  margin-bottom: 24px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.identify-label::after {
  content: '';
  flex: 1;
  height: 1px;
  background: var(--sage-light);
}

.identify-h2 {
  font-family: 'Cormorant Garamond', serif;
  font-size: 28px;
  font-weight: 400;
  color: var(--charcoal);
  margin-bottom: 32px !important;
  line-height: 1.3;
}

/* =========================================================
   9. LIST RESET FIXES
========================================================= */
#page-2724813135 ul {
  list-style: none !important;
  margin: 0 !important;
  padding: 0 !important;
}

#page-2724813135 li::before,
#page-2724813135 li::after,
#page-2724813135 li::marker {
  content: none !important;
  display: none !important;
}

/* Hero-Checkliste */
#page-2724813135 .reset-check-list {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

#page-2724813135 .reset-check-list li {
  display: flex !important;
  align-items: flex-start !important;
  gap: 14px !important;
  font-size: 15px;
  color: var(--mid);
  line-height: 1.5;
  background: none !important;
  border: 0 !important;
  box-shadow: none !important;
  position: static !important;
}

#page-2724813135 .reset-check-list li span {
  flex: 1;
}

#page-2724813135 .reset-check-icon {
  flex-shrink: 0;
  width: 22px;
  height: 22px;
  border-radius: 50%;
  background: var(--sage-light);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 1px;
}

#page-2724813135 .reset-check-icon svg {
  width: 10px;
  height: 10px;
  display: block;
}

/* Offer-Features */
#page-2724813135 .offer-features {
  margin: 32px 0 0 0 !important;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

#page-2724813135 .offer-features li {
  display: flex !important;
  align-items: center !important;
  gap: 14px !important;
  font-size: 15px;
  color: var(--mid);
  background: none !important;
  border: 0 !important;
  box-shadow: none !important;
  position: static !important;
}

#page-2724813135 .feature-dot {
  width: 8px;
  height: 8px;
  min-width: 8px;
  min-height: 8px;
  border-radius: 50%;
  background: var(--sage);
  flex-shrink: 0;
}

/* =========================================================
   10. WHY
========================================================= */
.why {
  background: var(--warm-white);
}

.why-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  align-items: center;
}

.why-points {
  display: flex;
  flex-direction: column;
  gap: 24px;
  margin-top: 40px;
}

.why-point {
  display: flex;
  gap: 20px;
  align-items: flex-start;
  padding: 20px 24px;
  border-left: 2px solid var(--sage-light);
  transition: border-color 0.2s;
}

.why-point:hover {
  border-color: var(--sage-deep);
}

.why-point-num {
  font-family: 'Cormorant Garamond', serif;
  font-size: 36px;
  font-weight: 300;
  color: var(--sage-light);
  line-height: 1;
  flex-shrink: 0;
}

.why-point-text strong {
  display: block;
  font-size: 15px;
  font-weight: 500;
  color: var(--charcoal);
  margin-bottom: 6px;
}

.why-point-text span {
  font-size: 14px;
  color: var(--soft);
  line-height: 1.6;
}

.why-transition {
  margin-top: 40px;
  padding: 28px 32px;
  background: var(--sage-deep);
  border-radius: var(--r);
  color: var(--cream);
  font-family: 'Cormorant Garamond', serif;
  font-size: 22px;
  font-weight: 300;
  font-style: italic;
  line-height: 1.4;
}

/* =========================================================
   11. DAYS
========================================================= */
.days {
  background: var(--cream);
}

.days-intro {
  margin-bottom: 60px;
}

.days-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 12px;
}

.day-card {
  background: var(--warm-white);
  border-radius: var(--r);
  padding: 20px 14px;
  text-align: center;
  border: 1px solid rgba(0,0,0,0.06);
  transition: all 0.2s;
  cursor: default;
}

.day-card:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.08);
  border-color: var(--sage-light);
}

.day-num {
  font-family: 'Cormorant Garamond', serif;
  font-size: 32px;
  font-weight: 300;
  color: var(--sage-deep);
  display: block;
  margin-bottom: 8px;
}

.day-label {
  font-size: 11px;
  font-weight: 500;
  color: var(--charcoal);
  letter-spacing: 0.04em;
  display: block;
  margin-bottom: 8px;
}

.day-text {
  font-size: 11px;
  color: var(--soft);
  line-height: 1.5;
}

.days-promise {
  margin-top: 40px;
  text-align: center;
  font-size: 14px;
  color: var(--soft);
  letter-spacing: 0.04em;
}

.days-promise strong {
  color: var(--charcoal);
}

/* =========================================================
   12. SCORE
========================================================= */
.score-section {
  background: var(--sage-deep);
  color: var(--cream);
}

.score-visual {
  margin-top: 60px;
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  gap: 40px;
  align-items: center;
}

.score-box {
  background: rgba(255,255,255,0.07);
  border: 1px solid rgba(255,255,255,0.12);
  border-radius: var(--r);
  padding: 40px;
  text-align: center;
}

.score-box .score-tag {
  font-size: 11px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: rgba(247,244,239,0.4);
  margin-bottom: 16px;
  display: block;
}

.score-num {
  font-family: 'Cormorant Garamond', serif;
  font-size: 72px;
  font-weight: 300;
  line-height: 1;
  color: var(--cream);
}

.score-num.after {
  color: var(--accent-light);
}

.score-sub {
  font-size: 13px;
  color: rgba(247,244,239,0.4);
  margin-top: 8px;
}

.score-arrow {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  color: var(--accent);
}

.score-arrow-line {
  width: 80px;
  height: 1px;
  background: linear-gradient(90deg, rgba(196,149,106,0.3), var(--accent));
  position: relative;
}

.score-arrow-line::after {
  content: '→';
  position: absolute;
  right: -8px;
  top: -10px;
  font-size: 20px;
  color: var(--accent);
}

.score-label {
  font-size: 12px;
  letter-spacing: 0.08em;
  color: rgba(247,244,239,0.4);
}

.score-message {
  margin-top: 48px;
  padding: 28px 40px;
  background: rgba(196,149,106,0.12);
  border-left: 3px solid var(--accent);
  border-radius: 0 var(--r) var(--r) 0;
  font-family: 'Cormorant Garamond', serif;
  font-size: 20px;
  font-style: italic;
  font-weight: 300;
  color: var(--accent-light);
  line-height: 1.6;
}

/* =========================================================
   13. AUDIENCE
========================================================= */
.audience {
  background: var(--cream);
}

.audience-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 32px;
  margin-top: 60px;
}

.audience-card {
  border-radius: var(--r);
  padding: 48px 40px;
  position: relative;
  overflow: hidden;
}

.audience-card.red {
  background: linear-gradient(135deg, #f0e8e8, #f7f0f0);
  border: 1px solid #e8d0d0;
}

.audience-card.yellow {
  background: linear-gradient(135deg, #f0ede0, #f7f4e8);
  border: 1px solid #e8dfc0;
}

.audience-h3 {
  font-family: 'Cormorant Garamond', serif;
  font-size: 26px;
  font-weight: 400;
  color: var(--charcoal);
  margin-bottom: 16px;
  line-height: 1.3;
}

.audience-text {
  font-size: 15px;
  color: var(--mid);
  line-height: 1.7;
  font-weight: 300;
}

/* =========================================================
   14. OBJECTIONS
========================================================= */
.objections {
  background: var(--warm-white);
}

.obj-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2px;
  background: var(--sage-light);
  border-radius: var(--r);
  overflow: hidden;
  margin-top: 48px;
}

.obj-item {
  background: var(--warm-white);
  padding: 36px 40px;
}

.obj-q {
  font-size: 14px;
  color: var(--soft);
  margin-bottom: 12px;
  font-style: italic;
}

.obj-q::before { content: '„'; }
.obj-q::after { content: '"'; }

.obj-a {
  font-size: 16px;
  color: var(--charcoal);
  font-weight: 400;
  line-height: 1.6;
}

.obj-a::before {
  content: '→ ';
  color: var(--sage-deep);
  font-weight: 500;
}

/* =========================================================
   15. OFFER
========================================================= */
.offer {
  background: var(--cream);
}

.offer-box {
  background: var(--warm-white);
  border: 1px solid rgba(0,0,0,0.08);
  border-radius: 16px;
  padding: 64px;
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 60px;
  align-items: center;
  box-shadow: 0 4px 40px rgba(0,0,0,0.05);
}

.offer-price-block {
  text-align: center;
  padding: 48px 40px;
  background: var(--sage-deep);
  border-radius: 12px;
  min-width: 220px;
}

.price-label {
  font-size: 11px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--sage-light);
  display: block;
  margin-bottom: 16px;
}

.price-amount {
  font-family: 'Cormorant Garamond', serif;
  font-size: 64px;
  font-weight: 300;
  color: var(--cream);
  line-height: 1;
  display: block;
}

.price-period {
  font-size: 12px;
  color: rgba(247,244,239,0.4);
  margin-top: 4px;
  display: block;
  margin-bottom: 32px;
}

.offer-price-block .btn-primary {
  width: 100%;
  text-align: center;
}

.trust-line {
  font-size: 11px;
  color: rgba(247,244,239,0.3);
  margin-top: 14px;
  display: block;
  letter-spacing: 0.06em;
}

/* =========================================================
   16. RESULT
========================================================= */
.result {
  background: var(--sage-deep);
}

.result-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 24px;
  margin-top: 56px;
}

.result-item {
  padding: 32px 24px;
  background: rgba(255,255,255,0.05);
  border-radius: var(--r);
  border: 1px solid rgba(255,255,255,0.08);
  transition: background 0.2s;
}

.result-item:hover {
  background: rgba(255,255,255,0.1);
}

.result-num {
  font-family: 'Cormorant Garamond', serif;
  font-size: 40px;
  font-weight: 300;
  color: var(--accent-light);
  display: block;
  margin-bottom: 12px;
}

.result-text {
  font-size: 14px;
  color: rgba(247,244,239,0.6);
  line-height: 1.6;
}

/* =========================================================
   17. FINAL CTA
========================================================= */
.final-cta {
  background: var(--cream);
  text-align: center;
  padding: 120px 40px;
}

.final-cta .section-h2 {
  margin-bottom: 20px;
}

.final-cta-sub {
  font-size: 16px;
  color: var(--soft);
  margin-bottom: 48px;
  font-weight: 300;
}

.final-cta .btn-primary {
  font-size: 16px;
  padding: 22px 56px;
}

/* =========================================================
   18. REVEAL / ANIMATION
========================================================= */
.reveal,
.reveal.visible {
  opacity: 1 !important;
  transform: none !important;
}

@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* =========================================================
   19. MOBILE
========================================================= */
@media (max-width: 900px) {
  .hero {
    grid-template-columns: 1fr;
    width: 100%;
    margin-left: 0;
  }

  .hero-right {
    display: none;
  }

  .hero-left {
    padding: 80px 32px 100px;
    min-height: 100vh;
  }

  .hero-left-inner {
    max-width: 100%;
    margin: 0;
  }

  section:not(.hero) {
    padding: 70px 24px;
  }

  .why-grid,
  .audience-grid,
  .obj-grid,
  .offer-box {
    grid-template-columns: 1fr;
  }

  .days-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
  }

  .offer-box {
    gap: 40px;
    padding: 40px 32px;
  }

  .result-grid {
    grid-template-columns: 1fr 1fr;
  }

  .score-visual {
    grid-template-columns: 1fr;
    gap: 24px;
  }

  .score-arrow {
    display: none;
  }
}
@media (max-width: 768px) {
  .hero-left {
    padding: 40px 20px 56px !important;
  }

  .hero-h1 {
    font-size: 52px !important;
    line-height: 1.02 !important;
    max-width: 100% !important;
  }

  .hero-sub {
    font-size: 15px !important;
    max-width: 100% !important;
  }

  .btn-primary {
    width: 100% !important;
    text-align: center !important;
  }
}

/* ]]> */
</style>


<script type="text/javascript">
//<![CDATA[
  // Scroll reveal
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((e, i) => {
      if (e.isIntersecting) {
        const delay = e.target.dataset.delay || 0;
        setTimeout(() => e.target.classList.add('visible'), delay);
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.12 });

  document.querySelectorAll('.reveal').forEach((el, i) => {
    // Stagger siblings
    const siblings = Array.from(el.parentElement.querySelectorAll('.reveal'));
    const idx = siblings.indexOf(el);
    el.dataset.delay = idx * 80;
    observer.observe(el);
  });
//]]>
</script>
